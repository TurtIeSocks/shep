[package]
name = "shep-core"
description = "Shared types, configuration, and wire protocol for the shep process manager"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
toml.workspace = true
serde-saphyr.workspace = true
# Unused directly — forces the transitive floor pins above into resolution (see root
# Cargo.toml comment). Required only for `-Z minimal-versions`; safe to drop once the
# corresponding upstream crate corrects its own declared floor.
annotate-snippets.workspace = true
anstyle.workspace = true
encoding_rs_io.workspace = true
pest.workspace = true
quote.workspace = true
syn.workspace = true
json5.workspace = true
regex.workspace = true
tokio-util.workspace = true
bytes.workspace = true

[dev-dependencies]
insta.workspace = true
tokio = { workspace = true, features = ["macros", "rt", "io-util"] }
futures-util.workspace = true
tempfile.workspace = true

[lints]
workspace = true
